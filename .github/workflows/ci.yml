name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-dejavu fonts-noto-color-emoji

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint
        run: ruff check tokcut tests

      - name: Type check
        run: mypy

      - name: Test
        run: pytest

  # VPS deploy — armed by the repo variable TOKCUT_DEPLOY=enabled
  # (Settings → Secrets and variables → Actions → Variables) plus the
  # VPS_HOST / VPS_USER / VPS_SSH_KEY secrets. Until those exist this
  # job is skipped. Server prep: deploy/bootstrap.sh (see docs/DEPLOY.md).
  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: >-
      github.ref == 'refs/heads/main' &&
      github.event_name == 'push' &&
      vars.TOKCUT_DEPLOY == 'enabled'
    concurrency: deploy            # never two deploys at once
    steps:
      - name: Deploy to VPS over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script_stop: true
          script: |
            cd /opt/tokcut
            git fetch origin main
            git reset --hard origin/main
            venv/bin/pip install -q -e ".[bot]"
            # drain: a restart mid-encode kills the take — wait for the
            # bot's .rendering marker to clear (up to ~20 min)
            for i in $(seq 1 60); do
              [ ! -f ~/.tokcut/work/.rendering ] && break
              echo "render in progress, waiting ($i/60)…"
              sleep 20
            done
            sudo /usr/bin/systemctl restart tokcut-bot
            systemctl --no-pager --lines=0 status tokcut-bot
